/* Write a function named countCharacters that takes a string as a parameter and returns the number of characters in the string. For example, countCharacters("hello") should return 5. */

const countCharacters = function (str) {
  return str.length;
};

console.log(countCharacters("hello"));
